# @brocolis/auth

Better Auth server + client + RBAC (5 portais: Consumer, Pharmacy, Supplier,
Business, Platform). Scaffold mínimo em F0; ligação real (scrypt OWASP, MFA
TOTP, invitations, org-switcher) na **F1 — IAM + Tenants**.