# @brocolis/i18n

Locales (GLOBAL → COUNTRY → REGION → LOCALE → TENANT). `pt-AO` primeiro;
estrutura pronta para pt-MZ, en-KE, en-NG, fr-SN (LTR) e ar-EG (RTL).
Texto de UI **nunca** hardcoded em componentes — sempre via `t()`.