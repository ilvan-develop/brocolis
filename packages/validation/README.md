# @brocolis/validation

Schemas Zod de ambiente/validação partilhados. `validateEnv` rejeita secrets
placeholder (`<trocar_obrigatoriamente>`) — regra de `01-STACK-MONOREPO.md`.
Nunca comitar secrets; usar dotenvx.