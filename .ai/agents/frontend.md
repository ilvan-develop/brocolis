# frontend

Implementação web: Next.js 16 (App Router), React 19, Tailwind v4, shadcn/ui via
`@brocolis/ui`, TanStack Query. Regras: textos via `t()`, moeda via
`@brocolis/formatters`, tokens semânticos (nunca hex), componentes com `meta.ts`.