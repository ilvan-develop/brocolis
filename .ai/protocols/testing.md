# TESTING

Estratégia: `blueprint/06-TESTES.md`. Unit (Vitest), integração (docker), contrato,
E2E web (Playwright) e mobile (Maestro). Cobertura ≥80% obrigatória.
Um único vitest no monorepo (override pinned).